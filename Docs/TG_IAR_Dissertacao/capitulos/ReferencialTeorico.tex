

\chapter{Referencial Teórico}\label{cap:teo}

Para iniciar a modelagem do sistema, é necessário descrever o mecanismo.
Segundo \citeauthor{Ford2008}, o sistema pode ser modelado utilizando como base um (** A definição seria essa, mas é necessário confirmar e encontrar um modelo mostrando a dinâmica do sistema) manipulador esférico de dois graus de liberdade.
Utilizando o modelo pela figura \ref{fig:pedestal}, o ponto fixo será o da base, dado pelo referencial de coordenadas $[x_0,y_0,z_0]$.
\begin{figure}[ht]
    \centering
    \caption{Modelo de pedestal com controle em azimute e elevação}
    \includegraphics[width=0.8\textwidth]{figs/pedestal_proxy.png}
    \legend{Fonte: Elaborado pelo autor (2026).}
    \label{fig:pedestal}
\end{figure}
Um motor será inserido na base da antena, permitindo a rotação em torno do eixo Z. O ângulo de rotação do azimute é dado por $\theta$.
O segundo motor está localizado na região na qual a antena se encaixa ao poste do pedestal. Suas coordenadas são dadas por $[x_1,y_1,z_1]$
É possível descrever o mecanismo do pedestal a partir de dois movimentos independentes, sendo um de azimute e um de elevação.
Para descrever uma semi-esfera, pode-se utilizar uma rotação de $0$ até $2\pi$ em azimute e $0$ até $\frac{\pi}{2} $ na elevação.

% \begin{equation}
% R_x(\theta) = \begin{pmatrix}
% 1 & 0 & 0 \\
% 0 & \cos\theta & -\sin\theta \\
% 0 & \sin\theta & \cos\theta
% \end{pmatrix}
% \label{eq:rotacao_x}
% \end{equation}
% \begin{equation}
% R_y(\theta) = \begin{pmatrix}
% \cos\theta & 0 & \sin\theta \\
% 0 & 1 & 0 \\
% -\sin\theta & 0 & \cos\theta
% \end{pmatrix}
% \label{eq:rotacao_y}
% \end{equation}
% \begin{equation}
% R_z(\theta) = \begin{pmatrix}
% \cos\theta & -\sin\theta & 0 \\
% \sin\theta & \cos\theta & 0 \\
% 0 & 0 & 1
% \end{pmatrix}
% \label{eq:rotacao_z}
% \end{equation}

\lipsum[34-36]